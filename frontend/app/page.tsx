import { Button } from "@/components/ui/button"

const page = () => {
  return (
    <div className="h-screen w-full flex justify-center items-center">
      <Button variant={'secondary'}>this is new button</Button>
    </div>
  )
}
export default page